# Copy to .env.local and fill in from the Terraform outputs.
# None of these are secrets: the Cognito app client is public and uses PKCE.
VITE_COGNITO_DOMAIN=REPLACE_ME.auth.us-east-1.amazoncognito.com
VITE_COGNITO_CLIENT_ID=REPLACE_ME
VITE_API_BASE_URL=http://localhost:8080
